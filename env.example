# Used by the Supabase CLI (supabase link / supabase db push), not by the
# static frontend — the frontend reads js/config.js instead (see js/config.example.js).

SUPABASE_PROJECT_REF=your-project-ref
SUPABASE_DB_PASSWORD=your-db-password
